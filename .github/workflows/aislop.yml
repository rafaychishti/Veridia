name: aislop — AI Code Quality Gate

on:
  push:
  pull_request:

jobs:
  aislop:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: aislop — AI Code Quality Gate
        uses: scanaislop/aislop@v0.16.1
